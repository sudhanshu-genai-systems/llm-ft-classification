# LoRA fine-tuning stack (Python 3.10+). Tested with Llama 3.2 3B + PEFT sequence classification.
# On SageMaker/GPU: install a CUDA-matched torch wheel from https://pytorch.org first, then pip install -r requirements.txt

# Core ML
torch==2.10.0
transformers==5.17.0
tokenizers==0.23.2
huggingface-hub==1.32.0
safetensors==0.8.0
accelerate==1.15.0
peft==0.21.0
bitsandbytes==0.50.2

# Datasets & I/O
datasets==5.0.1
pyarrow==25.0.1
sentencepiece==0.2.2
numpy==1.26.4
pandas==2.2.2

# Metrics & config
scikit-learn==1.4.2
scipy==1.17.1
pydantic==2.13.5
pydantic-core==2.46.5
pydantic-settings==2.15.0
python-dotenv==1.2.2

# Notebooks (optional on headless training jobs)
ipykernel==6.29.5
